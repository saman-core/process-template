package ${template.getPackageNameFormatted()}.${template.getProductNameFormattedLowerCase()}.${template.getNameFormattedLowerCase()}.client;

import ${template.getPackageNameFormatted()}.${template.getProductNameFormattedLowerCase()}.${template.getNameFormattedLowerCase()}.model.${template.getNameFormatted()}Model;
import io.smallrye.mutiny.Uni;

public interface ${template.getNameFormatted()}Client {

    Uni<${template.getNameFormatted()}Model> getById(Long id);

    Uni<${template.getNameFormatted()}Model> create(${template.getNameFormatted()}Model model);

    Uni<${template.getNameFormatted()}Model> update(${template.getNameFormatted()}Model model);

    Uni<Boolean> deleteById(Long id);
}