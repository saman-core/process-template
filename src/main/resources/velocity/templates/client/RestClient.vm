package ${template.getPackageNameFormatted()}.${template.getProductNameFormattedLowerCase()}.${template.getNameFormattedLowerCase()}.client.rest.microprofile;

import io.smallrye.mutiny.Uni;
import ${template.getPackageNameFormatted()}.${template.getProductNameFormattedLowerCase()}.${template.getNameFormattedLowerCase()}.model.${template.getNameFormatted()}Model;
import ${template.getPackageNameFormatted()}.${template.getProductNameFormattedLowerCase()}.${template.getNameFormattedLowerCase()}.model.${template.getNameFormatted()}RequestParameter;
import org.eclipse.microprofile.rest.client.annotation.RegisterClientHeaders;
import org.eclipse.microprofile.rest.client.inject.RegisterRestClient;
import jakarta.enterprise.context.ApplicationScoped;
import jakarta.ws.rs.*;
import jakarta.ws.rs.core.MediaType;
import io.samancore.common.model.PageData;
import io.samancore.common.page.PageRequest;

@ApplicationScoped
@RegisterRestClient(configKey = "cde-${template.getProductNameFormattedLowerCase()}-${template.getNameFormattedLowerCase()}-api")
@RegisterClientHeaders
@Path("/${template.getNameFormattedLowerCase()}s")
@Produces(MediaType.APPLICATION_JSON)
@Consumes(MediaType.APPLICATION_JSON)
public interface ${template.getNameFormatted()}RestClient {

    @GET
    @Path("/{id}")
    Uni<${template.getNameFormatted()}Model> getById(@PathParam("id") Long id);

    @POST
    @Path("/")
    Uni<${template.getNameFormatted()}Model> create(${template.getNameFormatted()}Model model);

    @PUT
    @Path("/")
    Uni<${template.getNameFormatted()}Model> update(${template.getNameFormatted()}Model model);

    @DELETE
    @Path("/{id}")
    Uni<Boolean> deleteById(@PathParam("id") Long id);

    @GET
    @Path("/")
    Uni<PageData<${template.getNameFormatted()}Model>> getAllByPage(${template.getNameFormatted()}RequestParameter requestParameter, PageRequest pageRequest);
}